[package]
name = "cloud-attachment"
version = "0.1.0"
edition = "2021"
description = "AttachmentState: the attach/detach state machine for a volume-like resource that gets connected to and disconnected from a compute-like resource, kept over its lifetime rather than once at creation."
license = "AGPL-3.0-only OR LicenseRef-Commercial"

[dependencies]
cloud-errors = { path = "../cloud-errors" }
