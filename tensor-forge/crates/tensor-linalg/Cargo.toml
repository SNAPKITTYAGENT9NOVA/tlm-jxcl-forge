[package]
name = "tensor-linalg"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Linear algebra on top of tensor-core: matmul, tensordot/einsum-style contraction, transpose/permute/reshape re-exports, and pure-Rust LU/QR/Cholesky/Jacobi-eigen/SVD decompositions for f32/f64."

[dependencies]
tensor-core = { workspace = true }
ndarray = { workspace = true }
num-traits = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
approx = { workspace = true }
