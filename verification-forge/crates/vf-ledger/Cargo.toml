[package]
name = "vf-ledger"
version = "0.1.0"
edition = "2021"
description = "ProofObject serialization and the cryptographic VERIFICATION_SEAL (SHA-256 over statement/proof/axiom/dependency hashes)."
license = "MIT"

[dependencies]
