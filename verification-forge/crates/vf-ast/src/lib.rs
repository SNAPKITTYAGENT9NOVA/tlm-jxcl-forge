//! Surface-syntax AST for the .vf theorem language (distinct from vf-core's internal Term representation).
//!
//! Status: scaffolded -- real implementation lands per
//! verification-forge's specified implementation order.
#![forbid(unsafe_code)]

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_placeholder() {}
}
