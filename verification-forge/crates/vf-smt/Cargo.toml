[package]
name = "vf-smt"
version = "0.1.0"
edition = "2021"
description = "SMT-LIB IR generation and a real solver backend, as an untrusted oracle only."
license = "AGPL-3.0-only OR LicenseRef-Commercial"

[dependencies]
