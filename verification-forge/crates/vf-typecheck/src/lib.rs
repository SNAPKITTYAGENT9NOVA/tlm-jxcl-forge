//! Bidirectional type-checking used during elaboration (untrusted -- see vf-kernel for the trusted checker).
//!
//! Status: scaffolded -- real implementation lands per
//! verification-forge's specified implementation order.
#![forbid(unsafe_code)]

#[cfg(test)]
mod tests {
    #[test]
    fn scaffold_placeholder() {}
}
