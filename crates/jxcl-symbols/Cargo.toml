[package]
name = "jxcl-symbols"
version = "0.1.0"
edition = "2021"
description = "The symbol table type (name -> address/section) shared by the assembler, object format, linker, and disassembler."
license = "MIT"

[dependencies]
jxcl-types = { path = "../jxcl-types" }
