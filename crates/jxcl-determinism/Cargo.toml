[package]
name = "jxcl-determinism"
version = "0.1.0"
edition = "2021"
description = "Machine state snapshot/restore and the determinism property-test harness."
license = "MIT"

[dependencies]
jxcl-machine = { path = "../jxcl-machine" }
jxcl-memory = { path = "../jxcl-memory" }
