[package]
name = "pq-proof-registry"
version = "0.1.0"
edition = "2021"
description = "A registry mapping scheme-id strings to boxed ProofScheme implementations, supporting future proof schemes beyond Groth16-Pedersen."
license = "MIT"

[dependencies]
pq-proof-types = { path = "../pq-proof-types" }
