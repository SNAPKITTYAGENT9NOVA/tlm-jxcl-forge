[package]
name = "jxcl-operands"
version = "0.1.0"
edition = "2021"
description = "The operand/addressing-mode model (register-direct, immediate, memory-indirect, etc.)."
license = "MIT"

[dependencies]
jxcl-types = { path = "../jxcl-types" }
jxcl-registers = { path = "../jxcl-registers" }
