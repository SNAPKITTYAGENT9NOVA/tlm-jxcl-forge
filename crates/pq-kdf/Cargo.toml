[package]
name = "pq-kdf"
version = "0.1.0"
edition = "2021"
description = "HKDF-SHA256 expansion of a KEM shared secret into an AES-256 key, with fixed domain separation."
license = "MIT"

[dependencies]
# (none yet -- external dependencies for this crate's
# real implementation are added when that batch lands,
# per docs/crates.toml's external_dependencies field)
