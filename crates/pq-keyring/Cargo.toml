[package]
name = "pq-keyring"
version = "0.1.0"
edition = "2021"
description = "The KeyRing data structure: an indexed set of key-pair entries."
license = "AGPL-3.0-only OR LicenseRef-Commercial"

[dependencies]
pq-envelope = { path = "../pq-envelope" }
