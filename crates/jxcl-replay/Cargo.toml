[package]
name = "jxcl-replay"
version = "0.1.0"
edition = "2021"
description = "Deterministically replays a recorded trace to reconstruct machine state at any step, without re-running the original inputs."
license = "MIT"

[dependencies]
jxcl-trace = { path = "../jxcl-trace" }
jxcl-machine = { path = "../jxcl-machine" }
jxcl-determinism = { path = "../jxcl-determinism" }
