# jxcl-bitops

Bitfield extraction/insertion and sign-extension helpers used by encoding, decoding, and the ALU.

## Architecture

**Owns:** extract_bits/insert_bits/sign_extend and related bit-level primitives.

**Category:** foundation · **Source:** extraction:jxcl/src/isa/opcodes.rs,jxcl/src/encoding/*

## Public API

`extract_bits`, `insert_bits`, `sign_extend`

## Dependencies

Workspace crates:

- `jxcl-types`

External crates:

*(none)*

## Testing

Planned test kinds: unit, property.

## Status

`planned` in `docs/crates.toml` -- see
`docs/CRATE_GENERATION_PLAN.md` for the implementation batch schedule.
